#include <iostream>
using namespace std;


struct Celda{

    int ele;
    Celda * sig;

}


// algoritmo de inserción
Celda * aux = L.primera;
while (aux->sig != p)
    aux = aux->sig

Celda * nueva = new Celda;
nueva->ele = 9;
nueva->sig = p;
aux->sig = nueva;

//algoritmo de borrado

Celda * aux = L.primera;
while (aux->sig != p)
    aux = aux->sig

aux->sig = p->sig;
delete p;





// Celdas doblemente enlazadas

Celda * aux = p->ant;

Celda * nueva = new Celda;
nueva -> ele = 9;
nueva->sig = p;
nueva->ant = aux
aux->sig = nueva;
p->ant = nueva;





// Operaciones de list de la STL

//Assign

//Insert (inserta detras de donde apunte el iterador)

//Erase

//Swap Intercambiar elementos entre listas

//Clear borra el contenido de la lista sin eliminar el elemento
